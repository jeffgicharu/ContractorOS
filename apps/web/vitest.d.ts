/// <reference types="@testing-library/jest-dom/vitest" />

// Augments Vitest's `expect` with the jest-dom matchers used in component
// tests under src/**/*.test.tsx. The runtime side is wired in vitest.setup.ts.
